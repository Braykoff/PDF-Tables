/** Source url for PDF.JS global worker. */
export const PDF_JS_GLOBAL_WORKER_SOURCE = "./pdfjs-5.4.394-legacy-dist/build/pdf.worker.mjs";

// MARK: Table sizing

/** Max number of columns. */
export const MAX_COLS = 50;

/** Default number of columns. */
export const DEFAULT_COLS = 4;

/** Default column size, px. */
export const DEFAULT_COL_SIZE = 25;

/** Minimum column size, px. */
export const MIN_COL_SIZE = 5;

/** Default height of a table, px. */
export const DEFAULT_TABLE_HEIGHT = 40;

/** Minimum height of a table, px. */
export const MIN_TABLE_HEIGHT = 2;

/** Multiplier for table canvas sizing (ie, resolution). */
export const TABLE_SCALE_FACTOR = 2;

/** Default table border width, px */
export const NORMAL_TABLE_BORDER_WIDTH = 1.5;

// MARK: Table rendering

/** Table border width while interacting, px. */
export const ACTIVE_TABLE_BORDER_WIDTH = 4;

/** Default table color. */
export const NORMAL_TABLE_COLOR = "navy";

/** Table color while interacting. */
export const ACTIVE_TABLE_COLOR = "blue";

/** Distance cursor can be from element to still be "hovering", px. */
export const TABLE_HOVER_BUFFER = 2;

/** Radius of the circles representing text boxes. */
export const TEXT_BOX_RADIUS = 2;

/** Color of the circles representing text boxes. */
export const TEXT_BOX_COLOR = "red";

/** Font size of index column label, px. */
export const LABEL_FONT_SIZE = 10;

/** Padding above and below index column label, px. */
export const LABEL_VERTICAL_PADDING = 3;
